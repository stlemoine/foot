{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipe creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('equipe') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
