{% extends 'FootBundle::layout.html.twig' %}

{% block title %} {{ parent() }}|user|{{'grid.liste'|trans}} {% endblock %}

{% block body %}

{% include ':Grid:grid_style.html.twig' %}

<div class="row">
    <div class="col-sm-2">
            {% include ':Grid:grid_search.html.twig' %}
    </div>
    <div class="col-sm-10">
        <h4>{{ 'grid.user.list'|trans}} <a href="{{ path('user_new') }}" class="btn btn-primary btn-xs">{{ 'create_a_new'|trans({},'messages') }}</a></h4>
        {{ grid(grid, ':Grid:index_grid.html.twig') }}
    </div>
</div>
{% endblock %}
