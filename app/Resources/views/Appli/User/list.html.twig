{% extends '::Appli/layout.html.twig' %}

{% block title %} {{ parent() }}|user|{{'grid.liste'|trans}} {% endblock %}

{% block appli_content %}
{% include ':Grid:grid_style.html.twig' %}
    
    <div class='row'>
        <div class='col-sm-2'>

            {% include ':Grid:grid_search.html.twig' %}
	
        </div>
        <div class='col-sm-10'>
            <div>
                <h4>
                    {{'menu.users.liste'|trans}} 
                    <a href="{{ path('user_new') }}" id="new" name="new" class="btn btn-primary btn-xs">{{ "create_a_new" | trans }}</a>
                </h4>
            </div>
            {{ grid(grid, ':Grid:index_grid.html.twig') }}
        </div>
    </div>  

{% endblock %}
