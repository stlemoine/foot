{% extends '::Appli/layout.html.twig' %}

{% block title %} {{ parent() }}|club|{{'grid.liste'|trans}} {% endblock %}


{% block appli_content %}

{% include ':Grid:grid_style.html.twig' %}

<div class="row">
    <div class="col-sm-2">
            {% include ':Grid:grid_search.html.twig' %}
    </div>
    <div class="col-sm-10">
        <h4>{{ 'menu.club.liste'|trans}} <a href="{{ path('club_new') }}" class="btn btn-primary btn-xs">{{ 'create_a_new'|trans({},'messages') }}</a></h4>
        {{ grid(grid, ':Grid:index_grid.html.twig') }}
    </div>
</div>
{% endblock appli_content %}

